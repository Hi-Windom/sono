- [x] v2.0 算法模块化包 `repair/` 已创建，包含 `repair_audio` 主函数
- [x] 自适应工作采样率：桌面端 48kHz，移动端使用原始采样率
- [x] 流式分块处理：桌面端 2 秒块 + 0.2 秒重叠，移动端 1 秒块 + 0.1 秒重叠
- [x] 块间 Hann 窗交叉淡入淡出合并正确
- [x] STFT 合并优化：频域处理组 A（去毛刺+去齿音+降噪）共享一次 STFT/ISTFT
- [x] STFT 合并优化：频域处理组 B（谐波增强+谐波丰富度）共享一次 STFT/ISTFT
- [x] 去削波使用三次样条插值重建，非硬裁剪
- [x] 降噪使用频谱减法 + Wiener 滤波平滑掩码，非硬门控
- [x] 去齿音使用频谱质心检测 + 多频段动态压缩
- [x] 去毛刺使用帧能量 + 频谱平坦度双指标检测
- [x] 去爆音使用多尺度能量检测（5ms/10ms/20ms）+ 交叉淡入淡出
- [x] 谐波增强使用向量化频段映射，无逐频点循环
- [x] 空间感增强包含相关性检测 + 低频居中保护
- [x] 响度归一化使用 K-加权响度测量（简化 ITU-R BS.1770）
- [x] 峰值限制使用软削波 tanh + lookahead 包络跟随器
- [x] 三频段压缩器（250Hz/4000Hz 分频点）+ 包络跟随器
- [x] 温暖度参数（偶次谐波注入）实现正确
- [x] 清晰度参数（高频搁架提升）实现正确
- [x] `resample_poly` 使用切片赋值避免形状不匹配
- [x] 每块处理完执行 `gc.collect()`
- [x] v2.0 在 `ALGORITHM_VERSIONS` 中注册，`mobile_compatible: True`
- [x] `PARAM_DEFINITIONS` 包含 `warmth` 和 `clarity`
- [x] `DEFAULT_VERSION` 更新为 `"v2.0"`
- [x] v2.0 的 4 种修复模式参数配置正确
- [x] `/algorithm-versions` API 在移动端返回 v2.0，在桌面端返回 v1.0/v1.1/v1.2/v2.0
- [x] 前端构建成功，无 TypeScript 错误
- [x] `release_android.tar.gz` 已重新打包
- [x] AI 检测算法命名已修正：v10→v1_0, v11→v1_1
- [x] v2.0 修复算法已模块化拆分为 `repair/` 包（11 个文件）
